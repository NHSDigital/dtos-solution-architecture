---
# id of your service, used for slugs and references in EventCatalog.
id: ValidateParticipant

# Display name of the Service, rendered in EventCatalog
name: Validate Participant

# Version of the Service
version: 0.0.1

# Short summary of your Service
summary: |
  Manages the participant, their enrolment in pathways as well as their episode creation and management

# Optional messages this service receives and it's version
receives:
  - id: ValidateParticipantProfile
    version: 0.0.1
 
# Optional messages this service sends and it's version
sends:
  - id: ParticipantProfileValidated
    version: 0.0.1

# Optional badges, rendered to UI by EventCatalog
badges:
  - content: New service
    backgroundColor: blue
    textColor: blue
---

## Overview

The participant manager provides crud operations to manage a participants enrolment in a pathway and provide access to their historic and ongoing episode data

<NodeGraph />
