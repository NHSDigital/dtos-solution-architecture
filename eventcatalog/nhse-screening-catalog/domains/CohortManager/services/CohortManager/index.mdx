---
# id of your service, used for slugs and references in EventCatalog.
id: CohortManager

# Display name of the Service, rendered in EventCatalog
name: Cohort Manager Service

# Version of the Service
version: 0.0.1

# Short summary of your Service
summary: |
  Manages all cohort related services in NSP validate participant profile against some validation rules once the eligible participants are received from national systems

# Optional messages this service receives and it's version
receives:
  - id: ValidateParticipantProfile
    version: 0.0.1
 
# Optional messages this service sends and it's version
sends:
  - id: ParticipantProfileValidated
    version: 0.0.1

# Optional badges, rendered to UI by EventCatalog
badges:
  - content: New service
    backgroundColor: blue
    textColor: blue
---

## Overview

Manages all cohort related services in NSP i.e. Adding/Removing eligible participant, validate participant

<NodeGraph />
