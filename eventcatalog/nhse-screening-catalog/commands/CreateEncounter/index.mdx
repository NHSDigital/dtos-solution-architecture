---
# id of your command, used for slugs and references in EventCatalog.
id: CreateEncounter

# Display name of the command, rendered in EventCatalog
name: Create Encounter

# Version of the command
version: 0.0.1

# Short summary of your command
summary: |
  Command with the intent to create an encounter for participant in a screening pathway

# Optional badges, rendered to UI by EventCatalog
badges:
  - content: New service
    backgroundColor: blue
    textColor: blue
---

## Overview

The `Create Enrolment` command represents intent to enrol a participant in a pathway over HTTP.

<NodeGraph />
