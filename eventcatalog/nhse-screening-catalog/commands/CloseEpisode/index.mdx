---
# id of your command, used for slugs and references in EventCatalog.
id: CloseEpisode

# Display name of the command, rendered in EventCatalog
name: Close Episode

# Version of the command
version: 0.0.1

# Short summary of your command
summary: |
  Command to Participant Manager

# Optional badges, rendered to UI by EventCatalog
badges:
  - content: New service
    backgroundColor: blue
    textColor: blue
---

## Overview

The `Close Episode` command represents intent to update the episode of a participant over HTTP.

<NodeGraph />
