---
# id of your command, used for slugs and references in EventCatalog.
id: SendInvitation
# Display name of the command, rendered in EventCatalog
name: Send Invitation

# Version of the command
version: 0.0.1

# Short summary of your command
summary: |
  Command to comms manager to communicate appointment booked to participant

# Optional badges, rendered to UI by EventCatalog
badges:
  - content: New service
    backgroundColor: blue
    textColor: blue
---

## Overview

The `SendInvitation` command represents intent to communicate participant.

<Schema />
