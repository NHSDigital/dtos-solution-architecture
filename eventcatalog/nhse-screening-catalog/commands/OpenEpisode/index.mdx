---
# id of your command, used for slugs and references in EventCatalog.
id: OpenEpisode

# Display name of the command, rendered in EventCatalog
name: Open Episode

# Version of the command
version: 0.0.1

# Short summary of your command
summary: |
  Command with the intent open a new episode for a given pathway enrolment

# Optional badges, rendered to UI by EventCatalog
badges:
  - content: New service
    backgroundColor: blue
    textColor: blue
---

## Overview

The `Open Episode` command represents intent to enrol a participant in a pathway over HTTP.

<NodeGraph />
